<%= form_for [user, task_list] do |f| %>
  <% if task_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this list from being saved:</h2>
      <ul>
        <% task_list.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <%= f.submit "Submit", class:"submit-button" %>
<% end %>
